---

Игра «Крестики-нолики» с генерацией промокодов и Telegram-ботом.
Игрок играет против компьютера, при победе получает уникальный 5-значный промокод, который также отправляется в Telegram-бот.

Проект состоит из двух частей:

* **Frontend** — React + TypeScript + CSS Modules
* **Backend** — Node.js + Express + Telegram Bot API
* **Взаимодействие**: фронтенд отправляет запросы на локальный сервер (`http://localhost:4000/api/send-result`), который отправляет уведомления в Telegram-бота.

---

# 🚀 Как запустить проект

## 1. 🔧 Установка и запуск Backend (Node.js + Express)

### 📁 Перейдите в папку `backend`:

```bash
cd backend
```

### 📦 Установите зависимости:

```bash
npm install
```

### ⚙️ Создайте файл окружения `.env`:

```env
BOT_TOKEN=ВАШ_ТОКЕН_БОТА
CHAT_ID=ВАШ_CHAT_ID
PORT=4000
```

Где:

* **BOT_TOKEN** — токен Telegram-бота, полученный от @BotFather
* **CHAT_ID** — ID чата, куда бот будет отправлять уведомления
* **PORT** — порт backend-сервера (по умолчанию 4000)

### ▶️ Запустите сервер:

```bash
npm start
```

### 🟢 Сервер успешно запустится на:

```
http://localhost:4000
```

---

# 💻 Как работает Backend

Backend поднимает локальный Express-сервер, который принимает POST-запрос с результатами игры:

```
POST /api/send-result
```

Пример тела запроса:

```json
{
  "result": "win",
  "code": "12345"
}
```

Backend делает:

Сервер отправляет данные в Telegram-бот через API `https://api.telegram.org/botTOKEN/sendMessage`.

❗ **Важно:** Сервер работает локально (`localhost:4000`), поэтому на продакшене потребуется VPS или прокси.

---

# 🎨 Как запустить Frontend (React + TypeScript)

Перейдите в папку frontend:

```bash
cd frontend
```

Установите зависимости:

```bash
npm install
```

Создайте `.env` файл:

```
VITE_BACKEND_URL=http://localhost:4000
```

Запуск проекта:

```bash
npm run dev
```

Открой игру:

```
http://localhost:5173
```

---

# 🕹️ Как работает игра

### Пользователь — X

Компьютер — O (движок AI построен на minimax)

### Логика:

* Игрок ходит → AI отвечает (с задержкой для реалистичности)
* На каждый ход игрока блокируются клики, пока бот «думает»
* После каждого хода выполняется проверка победы

### Итоги:

| Ситуация      | Что происходит                                           |
| ------------- | -------------------------------------------------------- |
| Победа игрока | Появляется промокод → отправка в Telegram                |
| Проигрыш      | Появляется модалка «Вы проиграли» → Telegram уведомление |
| Ничья         | Предлагается сыграть снова                               |

Вся логика UI работает локально, без сервера, кроме Telegram-уведомлений.

---

# 📦 Структура проекта

```
project/
│
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── styles/
│   │   ├── App.tsx
│   │   └── main.tsx
│   └── package.json
│
└── backend/
    ├── index.js
    ├── .env
    ├── package.json
```

---

# 🔐 Требования

* Node.js 18+
* npm или yarn
* Telegram-бот с токеном
* Локальный сервер (localhost)

---

Если нужно:

* задеплоить backend на VPS (Beget)
* сделать докеризацию
* подключить SSL
* добавить админ-панель с просмотром выданных промокодов

---

